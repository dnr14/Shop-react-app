import React from "react";
import StyledTitle from "components/style/StyledTitle";

const Title = ({ children }) => {
  return <StyledTitle>{children}</StyledTitle>;
};

export default Title;
